#include <vector>

class Solution {
public:
    // clang-format off
    long long minimumTankCapacity(
        int n,
        std::vector<std::vector<int>> &edges,
        std::vector<int> &fuel)
    {
        // clang-format on
        return 0;
    }
};
