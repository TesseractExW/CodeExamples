#include <vector>

class Solution {
public:
    // clang-format off
    std::vector<int> kthPosition(
        std::vector<int> &pos,
        std::vector<std::vector<int>> &queries)
    {
        // clang-format on
        // code here
        return std::vector<int>();
    }
};
